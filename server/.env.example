PORT=4000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=carsai_react

JWT_SECRET=change_this_in_production_please
JWT_REFRESH_SECRET=change_this_too_in_production
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=30d

CORS_ORIGIN=http://localhost:5173

CURRENCY_SYMBOL=MT
COMPANY_NAME=Carsai BMS

# ── Firebase Admin (server-side push notifications) ──────────────────────
# Base64-encode your service-account JSON: base64 -w0 service-account.json
FIREBASE_SERVICE_ACCOUNT=
